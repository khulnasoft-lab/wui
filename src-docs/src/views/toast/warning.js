import React from 'react';

import { WuiToast } from '../../../../src/components';

export default () => (
  <WuiToast
    title="Sometimes a title is enough!"
    color="warning"
    iconType="help"
  />
);
